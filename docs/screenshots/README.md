# Screenshots

Image files referenced by the **Screenshots** section of the top-level
[`README.md`](../../README.md). Drop the PNGs here using these exact names:

| File | Shows |
|------|-------|
| `dashboard.png` | Main dashboard: monitors list with status, per-check pills, and actions. |
| `reports.png` | Reports → Uptime: time-weighted uptime with per-outage Exclude/Re-include. |
| `edit-monitor.png` | Add/Edit monitor form: PEMs, scheduling, and selectable verification tests. |
| `crl-data.png` | CRL data history modal: per-check CRL metadata snapshots. |
| `history.png` | Status-change history modal. |

PNG is preferred. Keep them reasonably sized (≈1200px wide is plenty).
